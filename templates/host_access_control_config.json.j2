{
  "access_entries": [
{% for entry in _irods_cfg_access_entries %}
    {
      "address": "{{ entry.address }}",
      "group": "{{ entry.group }}",
      "mask": "{{ entry.mask }}",
      "user": "{{ entry.user }}"
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ]
}
